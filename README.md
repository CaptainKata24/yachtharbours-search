# Yacht Harbours — Search & Discovery UI

## Files
- `index.html` — complete app
- `vercel.json` — Vercel static site config

## Deploy (GitHub → Vercel)
1. Push both files to GitHub
2. Log into vercel.com → Add New Project
3. Import the repo → Deploy

## TODO for dev team
1. Search endpoint: update `searchBase` URL in `doSearch()`
2. Marina map pins: replace random offsets with real API coords in `initMap()`
3. Flag images: nothing to change, flagcdn.com works as-is
```

Name the file exactly:
```
README.md
